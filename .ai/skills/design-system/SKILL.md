---
name: design-system
description: Catalog of reusable UI components for this project (buttons, cards, menus, sections) with their variants and props. Use whenever building pages or sections, to reuse existing components instead of writing new markup.
---

# Design System

This catalog lists components that already exist in `resources/views/components/`.
ALWAYS check this list before building UI — reuse an existing component rather than creating a duplicate.
When you create a new reusable component, add a short entry here so it can be reused later.

## Components

<!--
Grows as components are built. Format per entry:

### Button — <x-ui.button>
Variants: primary (filled), secondary (outlined).
Props: variant; standard anchor attributes (href).
Usage: <x-ui.button variant="primary" href="...">Text</x-ui.button>
-->

_No components catalogued yet — populate as the build progresses._
